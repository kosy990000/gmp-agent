# 비밀키 — 절대 커밋 금지 (OPENAI_API_KEY, LANGSMITH_API_KEY)
.env

# 가상환경
.venv/

# 파이썬 캐시
__pycache__/
*.pyc

# 생성물 — 벡터DB(ingest.py 로 재생성 가능) + 대화 로그 DB
storage/

# 색인 원본 문서 (대용량 + 저작권 — 커밋하려면 이 줄 삭제)
data/gmp/*

# LangGraph Studio 로컬 상태
.langgraph_api/

# OS / 편집기
.DS_Store
.vscode/
.idea/
